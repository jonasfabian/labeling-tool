export class Sums {
  correct: number;
  wrong: number;
  total: number;

  constructor(correct: number, wrong: number, total: number) {
    this.correct = correct;
    this.wrong = wrong;
    this.total = total;
  }
}
